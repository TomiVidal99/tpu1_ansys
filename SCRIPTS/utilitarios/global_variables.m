% Se definen variables global.

global GENERAL_GRAPH_PATHS = "./figures/";
if ~exist(GENERAL_GRAPH_PATHS, "dir")
   mkdir(GENERAL_GRAPH_PATHS)
end

global AUDIO_FOLDER = "./AUDIOS_PROCESADOS";
if ~exist(GENERAL_GRAPH_PATHS, "dir")
   mkdir(GENERAL_GRAPH_PATHS)
end
